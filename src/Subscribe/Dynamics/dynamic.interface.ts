/* eslint-disable camelcase */
export interface dynamicInfo {
    card: string; // a json
    dynamic_id: number; // desc.dynamic_id
    timestamp: number; //
}
export interface dynamicRec {
    before_update: number;
    group_id: number;
    hit_count: number;
    latest_dynamic: number;
    name: string;
    uid: number;
}
